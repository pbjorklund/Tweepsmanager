$(".hero-unit").append("<%= escape_javascript(render partial: "page") %>");
$("#users").append("<%= escape_javascript(render :partial => "user", collection: @users ) %>");
$("#loading").html("<p>These are the people that you follow that don't follow you back:").attr("id","");
