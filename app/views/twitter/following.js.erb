$("#main").html("<%= escape_javascript(render partial: "page") %>");
$("#users").append("<%= escape_javascript(render :partial => "user", collection: @users ) %>");
$("#loading").html("<p>These are the people you are following:").attr("id","");
initPopover();
